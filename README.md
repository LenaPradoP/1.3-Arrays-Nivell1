# Ejercicios con Arrays en PHP

Este repositorio contiene cuatro ejercicios que exploran el manejo de arrays y funciones en PHP.

---

## 📄 Descripción - Enunciado de los ejercicios

### Ejercicio 1: Mostrar elementos de un array
1. Crear un array con 5 números enteros.
2. Mostrar cada número del array en pantalla, uno por uno.

### Ejercicio 2: Operaciones con el tamaño del array
1. Crear un array `$X` con valores iniciales `[10, 20, 30, 40, 50, 60]`.
2. Mostrar el tamaño inicial del array.
3. Eliminar un elemento del array y reorganizar los índices.
4. Mostrar el tamaño del array después de los cambios.

### Ejercicio 3: Verificar carácter en palabras de un array
1. Crear una función que reciba un array de palabras y un carácter.
2. La función devuelve `true` si todas las palabras contienen el carácter indicado, y `false` en caso contrario.

#### Ejemplo:
Si el array es `["hola", "Php", "Html"]`, retornará:
- `true` si se busca el carácter `"h"`.
- `false` si se busca el carácter `"l"`.

### Ejercicio 4: Array asociativo con datos personales
1. Crear un array asociativo que represente información personal, incluyendo:
   - Nombre.
   - Edad.
   - Email.
   - Comida favorita.
2. Mostrar los datos almacenados en el array.

---

## 💻 Tecnologías Utilizadas

- **PHP**: Lenguaje principal para implementar los ejercicios.

---

## 📋 Requisitos

- PHP >= 7.4 (declaración estricta de tipos utilizada en el código).
- Servidor local (XAMPP, MAMP o similar).

---

## 🛠️ Instalación

1. Clona este repositorio en tu máquina local:
   ```bash
   git clone <url-del-repositorio>
   ```
2. Accede al directorio del proyecto:
   ```bash
   cd proyecto-arrays-php
   ```
3. Configura tu servidor local para que apunte a la carpeta del proyecto.

---

## ▶️ Ejecución

1. Accede al archivo PHP principal desde tu navegador, por ejemplo:
   ```bash
   http://localhost/arrays.php
   ```
2. Se ejecutarán los cuatro ejercicios, mostrando los resultados en pantalla.

---

## 🌐 Despliegue

1. Sube los archivos del proyecto a tu servidor web.
2. Configura el servidor para que apunte a la carpeta del proyecto.
3. Accede al archivo desde tu navegador.

---

## 🤝 Contribuciones

¡Las contribuciones son bienvenidas! Por favor, sigue estos pasos para contribuir:

1. Realiza un fork del repositorio.
2. Crea una nueva rama para tus cambios:
   ```bash
   git checkout -b feature/nueva-funcionalidad
   ```
3. Realiza tus modificaciones y haz commit:
   ```bash
   git commit -m 'Añade nueva funcionalidad'
   ```
4. Sube los cambios a tu rama:
   ```bash
   git push origin feature/nueva-funcionalidad
   ```
5. Haz un pull request para que se revise tu contribución.

---

